{{#bs-form onSubmit=(action "submit") model=model as |form|}}

  {{form.element
    controlType="single-select"
    property="thing"
    label="Thing"
    options=(hash
      array=things
      labelKey="name"
    )}}

  {{bs-button defaultText="Submit" type="primary" buttonType="submit"}}

{{/bs-form}}